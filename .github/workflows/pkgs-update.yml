name: Update Packages Files
run-name: Updating Packages Files
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: rm ${{ github.workspace }}/Packages
      - run: rm ${{ github.workspace }}/Packages.bz2
      - run: sudo apt install bzip2 -y
      - run: sudo dpkg-scanpackages -m ${{ github.workspace }}/./debs | tee ${{ github.workspace }}/Packages
      - run: bzip2 ${{ github.workspace }}/Packages
      - run: mv Packages.bz2 ${{ github.workspace }}
